# Solo para desarrollo y pruebas
ALLOW_ALL_HOSTS=true
API_PROXY_TARGET=https://nonreflecting-nonapologetic-pennie.ngrok-free.dev/

# WebSocket propio (instancia B, TLS gestionado por Caddy).
# websocket.practicasoftware.fun es el del despliegue compartido del equipo.
VITE_WS_URL=wss://ws-233298.practicasoftware.fun

# API_RUTA_URL y VITE_API_RUTA_URL se eliminaron: rutas y puntos van por
# /api/ (gin-backend), que reenvia a api_rutas. Apuntar al servicio externo
# saltaba la autenticacion y mostraba los datos de otro despliegue.
